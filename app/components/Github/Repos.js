var React = require('react');

var Repos = React.createClass({
        render: function () {
            return (
              <div> Repos </div>
            );
        }
    }
);

module.exports = Repos;